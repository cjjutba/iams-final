# IAMS Edge Device Dependencies
# Raspberry Pi ARM-compatible Python packages

# Core dependencies
numpy==1.24.3
opencv-python==4.8.1.78
Pillow==10.1.0

# MediaPipe Face Detection (ARM-compatible)
# Note: MediaPipe supports ARM64 (aarch64) for Raspberry Pi 4/5
mediapipe==0.10.8

# Raspberry Pi Camera (Pi Camera Module)
# Note: picamera2 requires Raspberry Pi OS Bullseye or later
# picamera2>=0.3.12  # Uncomment if using Pi Camera Module

# HTTP client
httpx==0.25.2

# Environment variables
python-dotenv==1.0.0

# Optional: USB camera fallback uses OpenCV (already included above)

# Development dependencies (optional)
# pytest==7.4.3
# pytest-asyncio==0.21.1
